<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="QueryQuest.Views.MainPage"
             BackgroundColor="#0b1121"

            Shell.NavBarIsVisible="False"
            Shell.TabBarIsVisible="False"
            Shell.FlyoutBehavior="Disabled">
    <Grid
        ColumnDefinitions="*">
        <BoxView BackgroundColor="#0b1121"/>
        
        <ScrollView
            x:Name="MainView"
            Grid.Column="1">
        <VerticalStackLayout Padding="30,10" Spacing="25">
            <Image Source="queryquestlogo.png"
                HeightRequest="185"
                Aspect="AspectFit" />
            
            <Label Text="QUERY QUEST"
                TextColor="White"
                FontSize="32"
                FontAttributes="Bold"
                HorizontalOptions="Center" />

            <VerticalStackLayout Spacing="5" HorizontalOptions="Center">
                    <Label 
                    x:Name="CategoryLabel"
                    Text="{Binding CategoryLabelText}"   
                    FontSize="16">
                        <Label.Triggers>
                            <DataTrigger TargetType="Label" Binding="{Binding IsCategoryError}" Value="True">
                                <Setter Property="TextColor" Value="Red" />
                            </DataTrigger>
                            <DataTrigger TargetType="Label" Binding="{Binding IsCategoryError}" Value="False">
                                <Setter Property="TextColor" Value="Gold"/>
                            </DataTrigger>
                        </Label.Triggers>
                    </Label>
                    <Label 
                    x:Name="AmountLabel" 
                    Text="{Binding AmountLabelText}"
                    FontSize="16">
                    <Label.Triggers>
                    <DataTrigger TargetType="Label" Binding="{Binding IsAmountError}" Value="True">
                            <Setter Property="TextColor" Value="Red" />
                    </DataTrigger>
                    <DataTrigger TargetType="Label" Binding="{Binding IsAmountError}" Value="False">
                        <Setter Property="TextColor" Value="Gold" />
                    </DataTrigger>
                    </Label.Triggers>
                </Label>
                    <Label
                    x:Name="DifficultyLabel"
                    Text="{Binding DifficultyLabelText}"
                    FontSize="16">
                        <Label.Triggers>
                            <DataTrigger TargetType="Label" Binding="{Binding IsDifficultyError}" Value="True">
                                <Setter Property="TextColor" Value="Red"/>
                            </DataTrigger>
                            <DataTrigger TargetType="Label" Binding="{Binding IsDifficultyError}" Value="False">
                                <Setter Property="TextColor" Value="Gold"/>
                            </DataTrigger>
                        </Label.Triggers>
                    </Label>
                </VerticalStackLayout>
            <Button 
                IsEnabled="{Binding CanStartGame}"
                Text="Starta Quiz"
                Command="{Binding StartGameCommand}"
                CornerRadius="15" />
        </VerticalStackLayout>
    </ScrollView>
        <ImageButton 
            Source="burgermenu.png" 
            Command="{Binding ToggleMenuCommand}"
            BackgroundColor="Transparent"
            HeightRequest="30"
            WidthRequest="30"
            HorizontalOptions="Start" 
            VerticalOptions="Start"
            Aspect="AspectFit"
            Margin="15,10" />

        <BoxView x:Name="MenuOverlay" 
         BackgroundColor="Black" 
         Opacity="0" 
         InputTransparent="True">
            <BoxView.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding ToggleMenuCommand}" />
            </BoxView.GestureRecognizers>
        </BoxView>

        <Frame x:Name="SideMenu"
               Grid.Column="0"
               WidthRequest="280"
               HorizontalOptions="Start"
               TranslationX="-280" 
               BackgroundColor="#151d33"
               BorderColor="#2a3a5c"
               Padding="20"
               CornerRadius="0">
            <ScrollView VerticalScrollBarVisibility="Never">
                <VerticalStackLayout Spacing="20">
                    <Label Text="INSTÄLLNINGAR" 
                       TextColor="Gold" 
                       FontSize="24" 
                       FontAttributes="Bold" 
                       Margin="0,40,0,10"/>

                    <Button Text="Kategori ∇" Clicked="ToggleCategorySelection" CornerRadius="15" Margin="0,10,0,0"/>
                    <VerticalStackLayout x:Name="CategorySelection" IsVisible="{Binding UI.IsCategoryVisible}" Margin="10" Spacing="5">
                        <Button Text="Blandat" Command="{Binding SetCategoryCommand}" CommandParameter="" CornerRadius="15">
                            <Button.Triggers>
                                <DataTrigger TargetType="Button" Binding="{Binding UI.Settings.CategoryId}" Value="">
                                    <Setter Property="BackgroundColor" Value="Green"/>
                                </DataTrigger>
                            </Button.Triggers>
                        </Button>
                        <Button Text="Allmänbildning" Command="{Binding SetCategoryCommand}" CommandParameter="9" CornerRadius="15">
                        <Button.Triggers>
                                <DataTrigger TargetType="Button" Binding="{Binding UI.Settings.CategoryId}" Value="9">
                                <Setter Property="BackgroundColor" Value="Green"/>
                            </DataTrigger>
                        </Button.Triggers>
                            </Button>
                        <Button Text="Musik" Command="{Binding SetCategoryCommand}" CommandParameter="12" CornerRadius="15">
                        <Button.Triggers>
                                <DataTrigger TargetType="Button" Binding="{Binding UI.Settings.CategoryId}" Value="12">
                                <Setter Property="BackgroundColor" Value="Green"/>
                            </DataTrigger>
                        </Button.Triggers>
                        </Button>
                        <Button Text="Historia" Command="{Binding SetCategoryCommand}" CommandParameter="23" CornerRadius="15">
                        <Button.Triggers>
                                <DataTrigger TargetType="Button" Binding="{Binding UI.Settings.CategoryId}" Value="23">
                                <Setter Property="BackgroundColor" Value="Green"/>
                            </DataTrigger>
                        </Button.Triggers>
                        </Button>
                        <Button Text="Djur" Command="{Binding SetCategoryCommand}" CommandParameter="27" CornerRadius="15">
                        <Button.Triggers>
                                <DataTrigger TargetType="Button" Binding="{Binding UI.Settings.CategoryId}" Value="27">
                                <Setter Property="BackgroundColor" Value="Green"/>
                            </DataTrigger>
                        </Button.Triggers>
                        </Button>
                    </VerticalStackLayout>

                    <Button Text="Längd ∇" Clicked="ToggleAmountSelection" CornerRadius="15" Margin="0,10,0,0"/>
                    <VerticalStackLayout x:Name="AmountSelection" IsVisible="{Binding UI.IsAmountVisible}" Margin="10,0,10,10" Spacing="2">
                        <Button Text="10" Command="{Binding SetAmountCommand}" CommandParameter="10" HeightRequest="40">
                            <Button.Triggers>
                                <DataTrigger TargetType="Button" Binding="{Binding UI.Settings.Amount}" Value="10">
                                    <Setter Property="BackgroundColor" Value="Green"/>
                                </DataTrigger>
                            </Button.Triggers>
                        </Button>
                        <Button Text="20" Command="{Binding SetAmountCommand}" CommandParameter="20" HeightRequest="40">
                            <Button.Triggers>
                                <DataTrigger TargetType="Button" Binding="{Binding UI.Settings.Amount}" Value="20">
                                    <Setter Property="BackgroundColor" Value="Green"/>
                                </DataTrigger>
                            </Button.Triggers>
                        </Button>
                        <Button Text="30" Command="{Binding SetAmountCommand}" CommandParameter="30" HeightRequest="40">
                            <Button.Triggers>
                                <DataTrigger TargetType="Button" Binding="{Binding UI.Settings.Amount}" Value="30">
                                    <Setter Property="BackgroundColor" Value="Green"/>
                                </DataTrigger>
                            </Button.Triggers>
                        </Button>
                    </VerticalStackLayout>

                    <Button Text="Svårighetsgrad ∇" Clicked="ToggleDifficultySelection" CornerRadius="15" Margin="0,10,0,0"/>
                    <VerticalStackLayout x:Name="DifficultySelection" IsVisible="{Binding UI.IsDifficultyVisible}" Margin="10,0,10,10" Spacing="2">
                        <Button Text="Blandat" Command="{Binding SetDifficultyCommand}" BackgroundColor="#34495e" CommandParameter="" CornerRadius="15">
                        <Button.Triggers>
                            <DataTrigger TargetType="Button" Binding="{Binding UI.Settings.Difficulty}" Value="">
                                <Setter Property="BackgroundColor" Value="Green"/>
                            </DataTrigger>
                        </Button.Triggers>
                        </Button>
                        <Button Text="Lätt" IsEnabled="{Binding UI.Settings.CanSelectEasy}" Command="{Binding SetDifficultyCommand}" CommandParameter="easy" CornerRadius="15">
                            <Button.Triggers>
                                <DataTrigger TargetType="Button" Binding="{Binding UI.Settings.Difficulty}" Value="easy">
                                    <Setter Property="BackgroundColor" Value="Green"/>
                                </DataTrigger>
                            </Button.Triggers>
                        </Button>
                        <Button Text="Medel" IsEnabled="{Binding UI.Settings.CanSelectMedium}" Command="{Binding SetDifficultyCommand}" CommandParameter="medium" CornerRadius="15">
                            <Button.Triggers>
                                <DataTrigger TargetType="Button" Binding="{Binding UI.Settings.Difficulty}" Value="medium">
                                    <Setter Property="BackgroundColor" Value="Green"/>
                                </DataTrigger>
                            </Button.Triggers>
                        </Button>
                        <Button Text="Svårt" IsEnabled="{Binding UI.Settings.CanSelectHard}" Command="{Binding SetDifficultyCommand}" CommandParameter="hard" CornerRadius="15">
                            <Button.Triggers>
                                <DataTrigger TargetType="Button" Binding="{Binding UI.Settings.Difficulty}" Value="hard">
                                    <Setter Property="BackgroundColor" Value="Green"/>
                                </DataTrigger>
                            </Button.Triggers>
                        </Button>
                    </VerticalStackLayout>

                    <Button Text="STÄNG" 
                        Command="{Binding ToggleMenuCommand}" 
                        Margin="0,10"
                        BackgroundColor="#2a3a5c" />
                </VerticalStackLayout>
            </ScrollView>
        </Frame>
    </Grid>
</ContentPage>
